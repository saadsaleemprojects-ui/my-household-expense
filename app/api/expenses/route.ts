import { NextResponse } from "next/server";
import { sql } from "../../../lib/db";
export async function GET(){try{return NextResponse.json(await sql()`SELECT id, expense_date, merchant, category, amount, household_member FROM expenses ORDER BY expense_date DESC, id DESC`)}catch(e){return NextResponse.json({error:"Database error"},{status:500})}}
export async function POST(request:Request){try{const b=await request.json(),m=String(b.merchant||"").trim(),a=Number(b.amount);if(!m||!Number.isFinite(a)||a<=0)return NextResponse.json({error:"Invalid expense"},{status:400});const rows=await sql()`INSERT INTO expenses (expense_date,merchant,category,amount,household_member) VALUES (${b.date||new Date().toISOString().slice(0,10)},${m},${b.category||"Other"},${a},${b.member||"Saad"}) RETURNING id,expense_date,merchant,category,amount,household_member`;return NextResponse.json(rows[0],{status:201})}catch(e){return NextResponse.json({error:"Database error"},{status:500})}}
