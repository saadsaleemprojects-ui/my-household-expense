"use client";
import { useMemo, useState } from "react";
type Expense={id:number;date:string;merchant:string;category:string;amount:number;member:string};
const initial:Expense[]=[{id:1,date:"2026-09-01",merchant:"Grocery",category:"Food",amount:12500,member:"Saad"},{id:2,date:"2026-09-03",merchant:"Electricity",category:"Utilities",amount:8200,member:"Maryam"},{id:3,date:"2026-09-05",merchant:"Fuel",category:"Transport",amount:6000,member:"Saad"}];
const money=(n:number)=>new Intl.NumberFormat("en-PK",{style:"currency",currency:"PKR",maximumFractionDigits:0}).format(n);
export default function Home(){const [expenses,setExpenses]=useState(initial);const [tab,setTab]=useState("Dashboard");const [merchant,setMerchant]=useState("");const [amount,setAmount]=useState("");const total=useMemo(()=>expenses.reduce((s,e)=>s+e.amount,0),[expenses]);const budget=100000;function add(){const a=Number(amount);if(!merchant||!a)return;setExpenses([{id:Date.now(),date:new Date().toISOString().slice(0,10),merchant,category:"Other",amount:a,member:"Saad"},...expenses]);setMerchant("");setAmount("");}return <main><aside><h1>My Household<br/>Expense</h1>{["Dashboard","Expenses","Budget","Income","Allowances","Fixed Payables","Loans & Credit","History & AI","Settings"].map(x=><button className={tab===x?"active":""} onClick={()=>setTab(x)} key={x}>{x}</button>)}</aside><section><header><div><p className="eyebrow">HOUSEHOLD FINANCE</p><h2>{tab}</h2></div><div className="month">September 2026</div></header>{tab==="Dashboard"&&<><div className="cards"><Card label="Monthly spending" value={money(total)}/><Card label="Budget remaining" value={money(budget-total)}/><Card label="Transactions" value={String(expenses.length)}/></div><div className="panel"><h3>Recent expenses</h3>{expenses.map(e=><div className="row" key={e.id}><div><strong>{e.merchant}</strong><span>{e.category} · {e.member} · {e.date}</span></div><b>{money(e.amount)}</b></div>)}</div></>}{tab==="Expenses"&&<div className="panel"><h3>Add expense</h3><div className="form"><input value={merchant} onChange={e=>setMerchant(e.target.value)} placeholder="Merchant or description"/><input value={amount} onChange={e=>setAmount(e.target.value)} type="number" placeholder="Amount (PKR)"/><button className="add" onClick={add}>Add expense</button></div><h3>All expenses</h3>{expenses.map(e=><div className="row" key={e.id}><span>{e.merchant}</span><b>{money(e.amount)}</b></div>)}</div>}{!["Dashboard","Expenses"].includes(tab)&&<div className="panel"><h3>{tab}</h3><p>This module is ready for the next build phase. Data will be persisted with PostgreSQL before production use.</p></div>}</section></main>}
function Card({label,value}:{label:string,value:string}){return <div className="card"><span>{label}</span><strong>{value}</strong></div>}